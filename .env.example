# Admin user credentials for first DB initialization
ADMIN_EMAIL=admin@shiolplus.com
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Admin123!
# =====================================================
# SHIOL-PLUS Environment Configuration Template
# =====================================================
# Copy this file as .env and add your API keys
# =====================================================

# 🤖 GEMINI AI (Google AI Studio)
# https://makersuite.google.com/app/apikey
GEMINI_API_KEY=

# 🎰 MUSL API (Multi-State Lottery)
MUSL_API_KEY=

# � SMART POLLING SYSTEM (v3.1)
# Configure STEP 0: Smart Polling behavior
# Pipeline starts 11:05 PM ET (6 min after 10:59 PM draw)
POLLING_START_MINUTES_AFTER_DRAW=6      # Minutes after draw to start polling (default: 6)
POLLING_INTERVAL_SECONDS=120            # Seconds between API checks (default: 120 = 2 minutes)
POLLING_TIMEOUT_MINUTES=120             # Maximum polling duration (default: 120 = 2 hours)

# �🔐 JWT SECRET (minimum 32 characters)
JWT_SECRET_KEY=

# 💎 PREMIUM PASS SECRET
PREMIUM_PASS_SECRET_KEY=

# 💳 STRIPE (Payments)
# https://dashboard.stripe.com/apikeys
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_ID_ANNUAL=

# 📊 GOOGLE ANALYTICS 4 (Optional - for traffic analytics)
# 1. Create property at https://analytics.google.com
# 2. Get Property ID from Admin > Property > Property Details
# 3. Create Service Account at https://console.cloud.google.com
# 4. Download JSON credentials and save to config/ga4-credentials.json
# 5. Add service account email to GA4 property with "Viewer" role
GA4_PROPERTY_ID=
GA4_CREDENTIALS_PATH=config/ga4-credentials.json

# 🌍 ENVIRONMENT
ENVIRONMENT=development
DATABASE_PATH=./data/shiolplus.db
PORT=8000
HOST=0.0.0.0
LOG_LEVEL=INFO

# 🎯 PREDICTION ENGINE MODE
# Controls which prediction implementation to use
# Options: v1 (current StrategyManager), v2 (ML-based, not implemented), hybrid (not implemented)
PREDICTION_MODE=v1
